<h1>Add a new dose to <%= @cocktail.name %></h1>
<%= simple_form_for (@dose) do |f| %>
  <%= f.association :ingredient, label: 'Select an ingredient', collection: Ingredient.all.order(:name) %>
  <%= f.input :description %>

  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
